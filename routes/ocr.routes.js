const express = require('express');
const router = express.Router();

// OCR routes are handled within auth.routes.js via the register flow
// This file is a placeholder for any standalone OCR endpoints if needed

module.exports = router;
